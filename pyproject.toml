[project]
name = "openenv-email"
version = "1.0.0"
description = "Smart Email Triage Environment for OpenEnv"
authors = [
    { name = "Thrivikram" }
]

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openai",
    "openenv>=0.2.0"
]

[project.scripts]
server = "app:app"
